<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DemoLesson1">
        <activity
            android:name=".DemoWebViewActivity"
            android:exported="false" />
        <activity
            android:name=".DemoToggleButtonActivity"
            android:exported="false" />
        <activity
            android:name=".DemoToastActivity"
            android:exported="false" />
        <activity
            android:name=".DemoSeekBarActivity"
            android:exported="false" />
        <activity
            android:name=".DemoImageButtonActivity"
            android:exported="false" />
        <activity
            android:name=".DemoEditTextActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>